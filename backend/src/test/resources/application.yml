# Test configuration — overrides application.yml
# Testcontainers @ServiceConnection auto-configures spring.data.mongodb.uri
security:
  jwt:
    secret: test-secret-at-least-32-characters-long
    expiration-ms: 3600000
